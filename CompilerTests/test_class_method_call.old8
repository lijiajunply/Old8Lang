// 测试类方法调用
class Person {
    name <- ""

    func init(n) {
        this.name <- n
    }

    func sayHello() {
        PrintLine("Hello from " + this.name)
    }
}

person <- Person("Bob")
person.sayHello()
