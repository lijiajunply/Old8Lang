// 简单 Lambda 表达式测试

PrintLine("=== Lambda 表达式测试 ===")

// 基本 Lambda 表达式
add <- (a, b) -> a + b
result <- add(1, 2)
PrintLine("add(1, 2) = " + result)

// 单参数 Lambda 表达式
square <- x -> x * x
result <- square(5)
PrintLine("square(5) = " + result)
