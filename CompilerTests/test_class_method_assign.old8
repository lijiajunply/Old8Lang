// 测试类方法中的赋值
class Test {
    value <- 0

    func setValue(v) {
        this.value <- v
    }

    func getValue() {
        return this.value
    }
}

t <- Test()
t.setValue(42)
result <- t.getValue()
PrintLine(result)
