// 测试扩展方法中访问this的属性

extension string {
    func printThis() {
        print("直接打印this: " + this)
        len <- this.length()
        print("this.length(): " + len.ToStr())
        return len
    }
}

str <- "Hello"
result <- str.printThis()
print("返回值: " + result.ToStr())
