// 测试虚拟机模式的 extern 语句
// 这个测试只验证字节码生成，不实际调用 DLL

extern "msvcrt.dll" {
    func abs(x:int) -> int
    func sqrt(x:double) -> double
}

extern "kernel32.dll" stdcall {
    func GetCurrentProcessId() -> uint as GetPID
}

PrintLine("Extern 语句字节码生成测试完成")
