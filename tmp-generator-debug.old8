func gen(n:int) -> void {
    PrintLine("gen called with n = " + n.ToStr())
    while n < 3 {
        PrintLine("before yield, n = " + n.ToStr())
        yield n
        PrintLine("after yield, n = " + n.ToStr())
        n <- n + 1
        PrintLine("after increment, n = " + n.ToStr())
    }
    PrintLine("while loop ended, n = " + n.ToStr())
}

for i in gen(1) {
    PrintLine("received: " + i.ToStr())
}
