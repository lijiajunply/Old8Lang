// 测试IL验证机制的测试用例
// 这个测试用例包含一些可能生成无效IL代码的情况

// 正常情况：应该能通过IL验证
func normal_function() {
    a <- 123
    b <- 456
    c <- a + b
    PrintLine(c)
}

// 调用正常函数
normal_function()

// 注意：故意生成无效IL代码的情况会被编译器捕获，不会生成到最终的IL中
// 以下是一些可能导致IL验证失败的情况的示例注释

// 1. 堆栈不平衡的情况
// 例如：只压入一个值，但尝试弹出两个值
// 这种情况会在GenerateIl阶段被捕获

// 2. 无效的指令序列
// 例如：跳转到不存在的标签
// 这种情况会在GenerateIl阶段被捕获

// 3. 类型不匹配
// 例如：尝试将字符串与整数相加
// 这种情况会在语义分析阶段被捕获

// 4. 调用不存在的方法
// 例如：调用一个未定义的函数
// 这种情况会在语义分析阶段被捕获
