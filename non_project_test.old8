// 非项目模式测试
// 测试在没有 o8packages.json 的情况下使用全局包

print("=== 非项目模式包加载测试 ===")
print("测试在非项目目录下使用全局包功能")

// 测试导入标准库（应该始终可用）
print("\n--- 1. 测试标准库导入 ---")
import "MathLib"
print("sqrt(16) =", MathLib.sqrt(16))
print("abs(-5) =", MathLib.abs(-5))

// 导入更多标准库进行测试
print("\n--- 2. 测试全局包检测 ---")
import "OS"
print("系统信息:")
print(OS.OsInfo())

// 尝试导入全局第三方包
print("\n--- 3. 测试全局第三方包 ---")
print("尝试导入全局第三方包...")
import "TestGlobalLib"

print("从全局包调用函数:", TestGlobalLib.hello("Old8Lang"))
print("全局包版本:", TestGlobalLib.version)

print("\n=== 测试完成 ===")
print("非项目模式工作正常！")