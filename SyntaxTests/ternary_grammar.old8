// 测试三元表达式的语法解析

// 测试1: 简单的三元表达式
PrintLine(true ? 1 : 2)

// 测试2: 比较表达式作为条件
PrintLine(5 > 3 ? "大于" : "小于等于")

// 测试3: 赋值语句中的三元表达式
result <- true ? 1 : 2
PrintLine(result)

// 测试4: 赋值语句中的比较表达式作为条件的三元表达式
result <- 5 > 3 ? "大于" : "小于等于"
PrintLine(result)
