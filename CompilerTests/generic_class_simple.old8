// 编译器模式：最简单的泛型类测试

// 定义简单的泛型Box类
class Box<T> {
    private value:T

    func set(v:T) -> void {
        this.value <- v
    }

    func get() -> T {
        return this.value
    }
}

// 只测试int类型的Box
intBox <- Box<int>()
PrintLine("Box created successfully!")
