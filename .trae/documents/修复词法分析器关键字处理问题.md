1. 问题分析：词法分析器在处理关键字时，`Func`方法被调用了两次，导致同一个关键字被添加了两次标记。
2. 具体位置：`LangTokenizer.Tokenize`方法中，关键字处理逻辑（第333-340行）。
3. 修复方案：修改关键字处理逻辑，避免多次调用`Func`方法，或者修改`Func`方法使其只在需要时添加标记。
4. 修复步骤：

   * 替换当前的关键字处理逻辑，使用一个变量来存储匹配的关键字

   * 只调用一次`Func`方法来检查和添加标记

   * 确保每个关键字只被添加一次标记
5. 验证方法：运行应用程序，检查是否还会出现"期望 Identifier，但得到了 Import"的错误。

