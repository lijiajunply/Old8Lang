// 测试普通生成器是否工作

PrintLine("=== 测试普通生成器 ===")

func normalGenerator() {
    i <- 1
    while i <= 3 {
        yield i
        i <- i + 1
    }
}

gen <- normalGenerator()
for item in gen {
    PrintLine("Item: " + item.ToStr())
}

PrintLine("普通生成器测试完成")
