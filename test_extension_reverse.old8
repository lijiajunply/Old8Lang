// 测试完整的reverse方法

extension string {
    func reverse() {
        result <- ""
        i <- this.length() - 1
        while i >= 0 {
            result <- result + this[i]
            i <- i - 1
        }
        return result
    }
}

str <- "Hello"
print(str.reverse())
