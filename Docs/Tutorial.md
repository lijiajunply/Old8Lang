# Tutorial

```
import os // 引用
[import "net.dll" net net] //使用原生函数

a -> 1 // 赋值语句
1 <- b // 与 b -> 1是一样的
b -* a // b指向a , 即a,b共用一个a的Value
a *- b // 相反也是一样的
if a == b //if语句
   a <- 2
for a <- 1 , a < 3 ,a <- a + 1 //for循环
   b <- b + 1
while not (a == 4) //while循环
   a <- a + 1
func c() //方法
   d <- 4
class d //类
   d1 <- a
|*
asdf
asdf
asdf
asdf
*|
```
