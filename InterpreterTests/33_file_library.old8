// 测试 File 库函数
// 注意：文件操作测试需要谨慎，避免影响系统文件

import File

PrintLine("=== File 库函数测试 ===")

PrintLine("\n--- File 库功能说明 ---")
PrintLine("File 库提供以下功能:")
PrintLine("1. FileRead(path) - 读取文件内容")
PrintLine("2. FileReadLines(path) - 按行读取文件")
PrintLine("3. CompressZip(source, dest) - 压缩文件为ZIP")
PrintLine("4. ZipReadAll(zipPath) - 读取ZIP文件所有内容")

PrintLine("\n--- FileRead 函数测试 ---")
// 读取测试文件（需要准备测试文件）
PrintLine("FileRead 函数用于读取文件全部内容")
PrintLine("使用方法: content <- FileRead(\"path/to/file.txt\")")

// 示例（假设有测试文件）
// content <- FileRead("test_data.txt")
// PrintLine("文件内容: " + content)

PrintLine("\n--- FileReadLines 函数测试 ---")
PrintLine("FileReadLines 函数用于按行读取文件")
PrintLine("使用方法: lines <- FileReadLines(\"path/to/file.txt\")")

// 示例（假设有测试文件）
// lines <- FileReadLines("test_data.txt")
// PrintLine("文件总行数: " + Len(lines))
// for line in lines {
//     PrintLine("行内容: " + line)
// }

PrintLine("\n--- CompressZip 函数测试 ---")
PrintLine("CompressZip 函数用于压缩文件或目录")
PrintLine("使用方法: CompressZip(\"source/path\", \"dest.zip\")")

// 示例
// CompressZip("test_folder", "test.zip")
// PrintLine("已创建压缩文件: test.zip")

PrintLine("\n--- ZipReadAll 函数测试 ---")
PrintLine("ZipReadAll 函数用于读取ZIP文件的所有内容")
PrintLine("使用方法: contents <- ZipReadAll(\"archive.zip\")")

// 示例
// contents <- ZipReadAll("test.zip")
// PrintLine("ZIP 文件内容: " + contents)

PrintLine("\n--- File 库使用示例 ---")
PrintLine("// 完整的文件操作示例:")
PrintLine("// 1. 读取文件")
PrintLine("//    content <- FileRead(\"data.txt\")")
PrintLine("//")
PrintLine("// 2. 按行读取文件")
PrintLine("//    lines <- FileReadLines(\"data.txt\")")
PrintLine("//    for line in lines {")
PrintLine("//        PrintLine(line)")
PrintLine("//    }")
PrintLine("//")
PrintLine("// 3. 压缩文件")
PrintLine("//    CompressZip(\"folder\", \"archive.zip\")")
PrintLine("//")
PrintLine("// 4. 读取ZIP")
PrintLine("//    zipData <- ZipReadAll(\"archive.zip\")")

PrintLine("\n--- 注意事项 ---")
PrintLine("1. 文件路径可以是相对路径或绝对路径")
PrintLine("2. 读取文件前请确保文件存在")
PrintLine("3. 压缩操作会创建新的ZIP文件")
PrintLine("4. File 库不提供写入和删除功能")

PrintLine("\n✅ File 库函数测试完成")
PrintLine("注意：实际文件操作需要准备测试文件，并在安全环境中进行")
