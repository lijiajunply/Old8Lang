// 简单的同步函数测试

// 最简单的函数，只有return
func simpleReturn() {
    return 42
}

PrintLine("测试开始")
result <- simpleReturn()
PrintLine("函数执行完成")

// 检查结果
PrintLine("结果: " + result)
if result == 42 {
    PrintLine("测试通过！")
} else {
    PrintLine("测试失败：result = " + result)
}