// 测试简单的this关键字使用
class Person {
    name <- ""
    age <- 0
    
    func init(name, age) {
        this.name <- name
        this.age <- age
    }
}

// 创建对象并测试
alice <- Person("Alice", 30)
PrintLine(alice.name)
PrintLine(alice.age)