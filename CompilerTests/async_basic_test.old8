// 基本异步函数编译器测试

// 测试1：简单的异步函数声明
async func simpleAsync() {
    return "Success"
}

// 测试2：带参数的异步函数
async func asyncWithParams(x:int, y:int) {
    return x + y
}

// 测试3：带返回类型的异步函数
async func fetchData(url:string) {
    data <- "数据: " + url
    return data
}