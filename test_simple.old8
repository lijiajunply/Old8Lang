// 基本赋值和表达式
a <- 10
b <- a + 5
c <- b * 2

d <- not true
e <- -10

// 条件语句
if a > 5 {
    PrintLine("a is greater than 5")
}

// 循环语句
for i <- 1, i <= 3, i++ {
    PrintLine(i)
}

// 函数定义和调用
func add(x, y) -> {
    return x + y
}

result <- add(3, 4)
PrintLine(result)
