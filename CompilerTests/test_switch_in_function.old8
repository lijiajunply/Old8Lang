// 测试函数中的switch语句
PrintLine("开始测试")

func getDayName(day:int) {
    switch day {
        case 1 {
            return "星期一"
        }
        case 2 {
            return "星期二"
        }
        default {
            return "无效"
        }
    }
}

result <- getDayName(1)
PrintLine(result)
