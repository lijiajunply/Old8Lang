// 测试扩展方法中的索引访问

extension string {
    func getFirstChar() {
        print("准备访问索引...")
        c <- this[0]
        print("成功获取字符: " + c)
        return c
    }
}

str <- "Hello"
result <- str.getFirstChar()
print("返回值: " + result)
