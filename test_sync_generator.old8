// 测试同步生成器
func makeGenerator() {
    for i in [1~3] {
        PrintLine("Yielding: " + i.ToStr())
        yield i
    }
}

gen <- makeGenerator()
for item in gen {
    PrintLine("Got item: " + item.ToStr())
}
PrintLine("Done")
