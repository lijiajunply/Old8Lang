// vm_math_module.old8 - 数学模块示例

func add(a:int, b:int) -> int {
    return a + b
}

func multiply(a:int, b:int) -> int {
    return a * b
}

func square(x:int) -> int {
    return x * x
}

func main() -> void {
    // 模块初始化代码（可选）
    PrintLine("Math module loaded")
}
